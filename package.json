{
  "name": "todo_list",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c green,blue \"npm run server\" \"npm run client\"",
    "server": "cd backend && nodemon server.js",
    "client": "cd client-ui && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.1"
  }
}
